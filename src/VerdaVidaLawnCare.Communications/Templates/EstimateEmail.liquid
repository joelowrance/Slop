<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Estimate - VerdaVida Lawn Care</title>
    <style>
        /* Reset styles */
        body, table, td, p, a {
            margin: 0;
            padding: 0;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
        }
        
        body {
            background-color: #f4f4f4;
            color: #333333;
            line-height: 1.6;
        }
        
        /* Email container */
        .email-container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
        }
        
        /* Header */
        .header {
            background: linear-gradient(135deg, #2d8659 0%, #1e5d3e 100%);
            padding: 40px 30px;
            text-align: center;
        }
        
        .header h1 {
            color: #ffffff;
            font-size: 28px;
            font-weight: 700;
            margin: 0;
            letter-spacing: 1px;
        }
        
        /* Content */
        .content {
            padding: 40px 30px;
        }
        
        /* Greeting */
        .greeting {
            font-size: 18px;
            color: #333333;
            margin-bottom: 20px;
        }
        
        /* Introduction */
        .intro {
            font-size: 16px;
            color: #666666;
            margin-bottom: 30px;
            line-height: 1.8;
        }
        
        /* Estimate details box */
        .estimate-details {
            background-color: #f8f9fa;
            border-left: 4px solid #2d8659;
            padding: 20px;
            margin-bottom: 30px;
        }
        
        .estimate-details h2 {
            color: #2d8659;
            font-size: 20px;
            margin-bottom: 15px;
            font-weight: 600;
        }
        
        .detail-row {
            display: flex;
            justify-content: space-between;
            padding: 8px 0;
            border-bottom: 1px solid #e0e0e0;
        }
        
        .detail-row:last-child {
            border-bottom: none;
        }
        
        .detail-label {
            color: #666666;
            font-weight: 500;
        }
        
        .detail-value {
            color: #333333;
            font-weight: 600;
        }
        
        /* Line items table */
        .line-items {
            margin: 30px 0;
        }
        
        .line-items h2 {
            color: #2d8659;
            font-size: 20px;
            margin-bottom: 20px;
            font-weight: 600;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        
        thead {
            background-color: #2d8659;
            color: #ffffff;
        }
        
        th {
            padding: 12px;
            text-align: left;
            font-weight: 600;
            font-size: 14px;
        }
        
        td {
            padding: 12px;
            border-bottom: 1px solid #e0e0e0;
            font-size: 14px;
        }
        
        tbody tr:hover {
            background-color: #f8f9fa;
        }
        
        .text-right {
            text-align: right;
        }
        
        .text-center {
            text-align: center;
        }
        
        /* Totals section */
        .totals {
            margin-top: 30px;
            margin-bottom: 30px;
        }
        
        .total-row {
            display: flex;
            justify-content: space-between;
            padding: 10px 0;
            font-size: 16px;
        }
        
        .total-row.subtotal {
            color: #666666;
        }
        
        .total-row.tax {
            color: #666666;
        }
        
        .total-row.grand-total {
            border-top: 2px solid #2d8659;
            margin-top: 10px;
            padding-top: 15px;
            font-size: 24px;
            font-weight: 700;
            color: #2d8659;
        }
        
        /* Notes section */
        .notes {
            background-color: #f8f9fa;
            padding: 20px;
            margin: 30px 0;
            border-radius: 4px;
        }
        
        .notes h3 {
            color: #2d8659;
            font-size: 18px;
            margin-bottom: 10px;
        }
        
        .notes p {
            color: #666666;
            line-height: 1.8;
        }
        
        /* Terms section */
        .terms {
            background-color: #fff9e6;
            border-left: 4px solid #ffc107;
            padding: 20px;
            margin: 30px 0;
        }
        
        .terms h3 {
            color: #856404;
            font-size: 18px;
            margin-bottom: 10px;
        }
        
        .terms p {
            color: #856404;
            line-height: 1.8;
            font-size: 14px;
        }
        
        /* CTA Button */
        .cta-section {
            text-align: center;
            margin: 40px 0;
        }
        
        .cta-button {
            display: inline-block;
            background-color: #2d8659;
            color: #ffffff !important;
            padding: 16px 40px;
            text-decoration: none;
            border-radius: 4px;
            font-weight: 600;
            font-size: 16px;
            transition: background-color 0.3s;
        }
        
        .cta-button:hover {
            background-color: #1e5d3e;
        }
        
        /* Footer */
        .footer {
            background-color: #2d8659;
            color: #ffffff;
            padding: 30px;
            text-align: center;
            font-size: 14px;
        }
        
        .footer p {
            margin: 5px 0;
            color: #ffffff;
        }
        
        .footer a {
            color: #ffffff;
            text-decoration: underline;
        }
        
        /* Responsive */
        @media only screen and (max-width: 600px) {
            .content {
                padding: 20px 15px;
            }
            
            .header {
                padding: 30px 20px;
            }
            
            .header h1 {
                font-size: 24px;
            }
            
            table {
                font-size: 12px;
            }
            
            th, td {
                padding: 8px;
            }
            
            .detail-row {
                flex-direction: column;
            }
            
            .detail-value {
                margin-top: 5px;
            }
        }
    </style>
</head>
<body>
    <div class="email-container">
        <!-- Header -->
        <div class="header">
            <h1>VerdaVida Lawn Care</h1>
        </div>
        
        <!-- Content -->
        <div class="content">
            <!-- Greeting -->
            <div class="greeting">
                Hello {{ CustomerFirstName }},
            </div>
            
            <!-- Introduction -->
            <div class="intro">
                Thank you for considering VerdaVida Lawn Care for your lawn care needs. We're excited to provide you with a detailed estimate for your project.
            </div>
            
            <!-- Estimate Details -->
            <div class="estimate-details">
                <h2>Estimate Details</h2>
                <div class="detail-row">
                    <span class="detail-label">Estimate Number:</span>
                    <span class="detail-value">{{ EstimateNumber }}</span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Date:</span>
                    <span class="detail-value">{{ EstimateDate | date: "%B %d, %Y" }}</span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Valid Until:</span>
                    <span class="detail-value">{{ ExpirationDate | date: "%B %d, %Y" }}</span>
                </div>
            </div>
            
            <!-- Line Items -->
            <div class="line-items">
                <h2>Service Details</h2>
                <table>
                    <thead>
                        <tr>
                            <th>Description</th>
                            <th class="text-center">Quantity</th>
                            <th class="text-right">Unit Price</th>
                            <th class="text-right">Total</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for item in LineItems %}
                        <tr>
                            <td>{{ item.Description }}</td>
                            <td class="text-center">{{ item.Quantity }}</td>
                            <td class="text-right">${{ item.UnitPrice | round: 2 }}</td>
                            <td class="text-right">${{ item.LineTotal | round: 2 }}</td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
            
            <!-- Totals -->
            <div class="totals">
                <div class="total-row subtotal">
                    <span>Subtotal:</span>
                    <span>${{ Subtotal | round: 2 }}</span>
                </div>
                {% if TaxAmount > 0 %}
                <div class="total-row tax">
                    <span>Tax:</span>
                    <span>${{ TaxAmount | round: 2 }}</span>
                </div>
                {% endif %}
                <div class="total-row grand-total">
                    <span>Total:</span>
                    <span>${{ TotalAmount | round: 2 }}</span>
                </div>
            </div>
            
            <!-- Notes -->
            {% if Notes != blank %}
            <div class="notes">
                <h3>Notes</h3>
                <p>{{ Notes }}</p>
            </div>
            {% endif %}
            
            <!-- Terms -->
            {% if Terms != blank %}
            <div class="terms">
                <h3>Terms & Conditions</h3>
                <p>{{ Terms }}</p>
            </div>
            {% endif %}
            
            <!-- Call to Action -->
            <div class="cta-section">
                <a href="#" class="cta-button">Accept This Estimate</a>
            </div>
        </div>
        
        <!-- Footer -->
        <div class="footer">
            <p><strong>VerdaVida Lawn Care</strong></p>
            <p>Professional Lawn Care Services</p>
            <p>If you have any questions, please don't hesitate to contact us.</p>
            <p style="margin-top: 15px;">
                <a href="mailto:support@verdevida.com">support@verdevida.com</a>
            </p>
        </div>
    </div>
</body>
</html>

